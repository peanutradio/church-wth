# 🌐 도메인 연결 가이드 (Vercel)

보유하신 도메인(`www.wethechurch.or.kr`)을 Vercel에 연결하는 방법입니다.

## 1단계: Vercel에서 도메인 추가

1. [Vercel 대시보드](https://vercel.com/dashboard)에 접속하여 `church-wth` 프로젝트를 클릭합니다.
2. 상단 메뉴의 **Settings**를 클릭합니다.
3. 왼쪽 메뉴의 **Domains**를 클릭합니다.
4. 입력창에 `www.wethechurch.or.kr`을 입력하고 **Add** 버튼을 누릅니다.
   - *참고: `wethechurch.or.kr` (www 없는 주소)도 함께 추가하는 것이 좋습니다. Vercel이 자동으로 리다이렉트 설정을 제안할 것입니다.*

## 2단계: 도메인 구입처에서 DNS 설정 (가장 중요!)

Vercel에서 도메인을 추가하면, **"Invalid Configuration"**이라는 에러 메시지와 함께 **필요한 DNS 레코드 값**을 보여줍니다.
이 값을 도메인을 구입하신 사이트(가비아, 후이즈, 호스팅케이알 등)의 관리 페이지에 입력해야 합니다.

### 설정해야 할 값 (예시)
Vercel 화면에 표시된 값을 정확히 입력해야 하지만, 보통 다음과 같습니다.

| 레코드 타입 | 호스트(Host) | 값(Value) / 목적지 |
|------------|-------------|-------------------|
| **CNAME**  | `www`       | `cname.vercel-dns.com` |
| **A**      | `@` (또는 빈칸) | `76.76.21.21` (루트 도메인 연결 시) |

**설정 방법:**
1. 도메인 구입처(예: 가비아) 로그인 > **DNS 관리** 또는 **네임서버 설정** 메뉴로 이동
2. **레코드 추가** 버튼 클릭
3. 위 표(또는 Vercel이 알려준 값)대로 입력하고 **저장**

## 3단계: 연결 확인

DNS 설정 후 전 세계에 반영되기까지 **최대 24시간**이 걸릴 수 있습니다. (보통 10분~1시간 내에 됩니다.)
Vercel Domains 페이지에서 파란색 체크 표시(✅)가 뜨면 연결이 완료된 것입니다.

---

## ⚠️ 주의사항: 소셜 로그인 설정 변경

도메인이 바뀌면 **구글/카카오 로그인 설정도 변경**해야 합니다!
기존에는 `church-wth.vercel.app`만 허용되어 있었기 때문에, 새 도메인에서는 로그인이 안 될 수 있습니다.

### 1. Supabase 설정 변경
1. Supabase Dashboard > Authentication > URL Configuration
2. **Site URL**: `https://www.wethechurch.or.kr`로 변경
3. **Redirect URLs**: `https://www.wethechurch.or.kr/**` 추가

### 2. 구글 클라우드 콘솔 (Google Cloud Console)
1. [Google Cloud Console](https://console.cloud.google.com/) 접속
2. API 및 서비스 > 사용자 인증 정보 (Credentials)
3. OAuth 2.0 클라이언트 ID 수정
4. **승인된 리디렉션 URI**에 `https://www.wethechurch.or.kr` 관련 주소는 추가할 필요 없음 (Supabase가 처리함).
   - *단, 승인된 자바스크립트 원본에 새 도메인을 추가해야 할 수도 있습니다.*

### 3. 카카오 개발자 센터 (Kakao Developers)
1. [Kakao Developers](https://developers.kakao.com/) 접속
2. 내 애플리케이션 > 앱 설정 > 플랫폼 > Web
3. **사이트 도메인**에 `https://www.wethechurch.or.kr` 추가
4. 카카오 로그인 > Redirect URI에 `https://www.wethechurch.or.kr/oauth/callback` 같은 형식이 있다면 추가 (Supabase 방식이면 보통 불필요)
